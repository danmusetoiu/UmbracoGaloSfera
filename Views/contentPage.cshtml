@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels
@using Umbraco.Extensions
@using Umbraco.Cms.Core.Security;
@inject IMemberManager _memberManager;
@inject Microsoft.Extensions.Configuration.IConfiguration Configuration;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.ContentPage>
@{
    Layout = "master.cshtml";
}

@Html.Partial("~/Views/Partials/SectionHeader.cshtml")

<section class="section--content-center">
    <div>
        <br />
        <p class="fs-2 mt-0 mb-0">GENERAREA FORMULARULUI</p><br />
        <p class="mt-0 mb-0" style="font-size:6.475em;"><b>ANAF 230</b></p>
    </div>
    <div>

    </div>
    
</section>

<section class="section--content-center">
    <div>
    <p class="fs-4"><b>Despre formular</b></p><br />
    <p class="fs-5" style="margin-inline:600px">Acest formular permite contribuabililor sa redirectioneze până la 3,5% din impozitul pe venit datorat statului către o organizație neguvernamentală</p>
        <button type="button" class="btn btn-primary rounded-pill btn-lg" style="margin-top:20px; margin-bottom:20px">Incarca formularul</button><br />
        <button type="button" class="btn btn-primary rounded-pill btn-lg" style="margin-top:20px; margin-bottom:20px" onclick="downloadPDF()">Descarcă PDF</button>

    </div>
</section>

@* <script>
    // Obține valoarea configurată din appsettings.json
    const documentePath = '@Configuration["Media:DocumentePath"]';

    function downloadPDF() {
        const link = document.createElement('a');
        link.href = `media/ibeh0jph/230-var1.pdf`; // Numele fișierului
        link.download = 'document.pdf'; // Numele fișierului la descărcare
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
    }
</script> *@
<script>
    const documentePath = '@Configuration["Media:DocumentePath"]' || ''; // Valoarea configurată

    function downloadPDF() {
        const link = document.createElement('a');
        link.href = 'https://localhost:44323/media/ibeh0jph/230-var1.pdf'   //`${documentePath}/ibeh0jph/230-var1.pdf`; // URL complet
        link.download = 'document.pdf';
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
    }
</script>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@using Newtonsoft.Json
@using Umbraco.Extensions;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.Blog>;


@{
    Layout = "master.cshtml";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Leaflet Map</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.6.0/dist/leaflet.css"
          integrity="sha512-xwE/Az9zrjBIphAcBb3F6JVqxf46+CDLwfLMHloNu6KEQCAWi6HcDUbeOfBIptF7tcCzusKFjFw2yuvEpDL9wQ=="
          crossorigin="">
    <link rel="stylesheet" href="~/css/Layout.css">
    <link rel="stylesheet" href="~/css/Noutati.css">
    <style type="text/css">
        body {
            margin: 0;
            padding: 0;
        }

        #map {
            width: 100vw;
            height: 100vh;
        }
    </style>
</head>
@Html.Partial("~/Views/Partials/SectionHeader.cshtml")

<body>
    <div class="right-menu">
        <div class="top-icon">
            <div class="icon-circle">
                <span class="icon"><a href="../"><img src="/media/ftfn2ljm/icon-galosfera-circle.svg" alt="Galosfera" /></a></span>
            </div>            
        </div>

        <div class="icon-button">
            <span class="icon"><a href="/Contact"><img src="/media/rhhnndxi/mesaje2.svg" alt="Mesaje" /></a></span>
        </div>
        <div class="icon-button icon-albastru">
            <span class="icon"><a href="/Noutati"><img src="/media/vz4lddxh/stiri2.svg" alt="Știri" /></a></span>
        </div>
        <div class="icon-button">
            <span class="icon"><a href="/Biblioteca"><img src="/media/vaahbacz/notificari2.svg" alt="Notificări" /></a></span>
        </div>
        <div class="icon-button">
            <span class="icon"><a href="/Evenimente"><img src="/media/ja1d354d/calendar2.svg" alt="Calendar" /></a></span>
        </div>
    </div>
    <div class="container text-center">
    
            @* <h1>Ultimele stiri</h1> *@


        <div class="row">

            <div class="col-lg-8 col-md-6 coloana-noutati-stanga">
                @foreach (Blogpost stire in Model.Children<Blogpost>())
                {
                    <div class="row d-none stire-@stire.Id">

                        <div class="col-4">
                            @if (stire.FotoStire != null)
                            {
                                <img src="@stire.FotoStire.Url()" alt="@stire.Name" class="blog-image" />
                            }
                        </div>
                        <div class="col-8">
                            <div class="container d-flex">
                                <h3 class="text-albastru"> @stire.Name </h3>
                            </div>
                            <div class="container text-justify-interword">
                                @stire.Excerpt
                            </div>
                            
                            <div class="container">
                                <a href="@stire.Url()">Mai mult....</a>
                            </div>
                        </div>
                        <br />
                    </div>
                }
            </div>
            <div class="col-lg-4 col-md-6 coloana-noutati-dreapta">
                @foreach (Blogpost stire in Model.Children<Blogpost>())
                {
                    <div class="row stire-@stire.Id">
                        <div class="col-4">
                            @if (stire.FotoStire != null)
                            {
                                <img src="@stire.FotoStire.Url()" alt="@stire.Name" class="blog-image" />
                            }
                        </div>
                        <div class="col-8 d-flex">
                            <div class="container text-align-start">
                                <h3 class="text-albastru col-12"> @stire.Name </h3>
                                <span class="col-12">@stire.UpdateDate.ToString("dd.MM.yyyy") | @stire.UpdateDate.ToString("HH:mm")</span>
                            </div>
                        </div>
                    </div>
                    <br />
                }
            </div>
        </div>  
@*                 @foreach (Blogpost stire in Model.Children<Blogpost>())
                {
                    <div class="container">
                        <h3> @stire.Name </h3>
                    </div>
                    <div class="container">
                        @stire.Excerpt
                    </div>
                    <div class="container">
                        <a href="@stire.Url()">Mai mult....</a>
                    </div>
                    <br />
                } *@

    </div>


    <div class="container text-center text-danger">
        <h3>Localizare sedii Grupuri de Acțiune Locală</h3>
    </div>
    
    <div id="map" class="container text-center">
    <script src="https://unpkg.com/leaflet@1.6.0/dist/leaflet.js"
            integrity="sha512-gZwIG9x3wUXg2hdXF6+rVkLF/0Vi9U8D2Ntg4Ga5I5BZpVkVxlJWbSQtXPSiUTtC0TjtGOmxa1AJPuV0CPthew=="
            crossorigin=""></script>
  
    @{
        var list = Model.Children<Blogpost>().Select(b => new { b.LocalizareGal.Marker.Latitude, b.LocalizareGal.Marker.Longitude, b.WebsiteGal }).ToList();
    }

  
    <script>
        const lista = JSON.parse('@(Html.Raw(JsonConvert.SerializeObject(list, Formatting.None)))');
        var map = L.map('map').setView([45.121, 25.3321], 7);
        L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
            maxZoom: 19,
            attribution: '&copy; <a href="http://www.openstreetmap.org/copyright">OpenStreetMap</a>'
        }).addTo(map);

        lista.forEach(o => L.marker([o.Latitude, o.Longitude]).bindPopup("<b>Detalii lansare apel la:</b> <br />" + o.WebsiteGal).openPopup().addTo(map));

    </script>
    <script>
            function AfiseazaStireaSelectata(elementApasat){
                if (elementApasat === undefined) { 
                    return;
                }
                const coloanaStanga = document.querySelector('.coloana-noutati-stanga');
                const randuriStanga = coloanaStanga.querySelectorAll('.row');
                const stireAfisata = coloanaStanga.querySelectorAll('.row:not(.d-none)')[0];
                stireAfisata.classList.add('d-none');
                const identificatorStire = Array.from(elementApasat.classList).find(classname => classname.startsWith('stire-'));
                const stireDeAfisat = coloanaStanga.querySelectorAll('.row.' + identificatorStire)[0];
                stireDeAfisat.classList.remove('d-none');
            }
            window.addEventListener("load", (event) => {
                const coloanaStanga = document.querySelector('.coloana-noutati-stanga');
                const randuriStanga = coloanaStanga.querySelectorAll('.row');
                randuriStanga[0].classList.remove('d-none');
                const coloanaDreapta = document.querySelector('.coloana-noutati-dreapta');
                const randuriDreapta = coloanaDreapta.querySelectorAll('.row');
                randuriDreapta.forEach((element) => element.addEventListener('click', () => AfiseazaStireaSelectata(element)));
            });
    </script>
    </div>
    

</body>
</html>
